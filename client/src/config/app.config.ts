// Application Configuration
// Set USE_MOCK_API to true to work on frontend without backend
// Set to false to use real API

export const APP_CONFIG = {
  USE_MOCK_API: true, // Toggle this to switch between mock and real API
  API_BASE_URL: '/api',
  MOCK_DELAY: 500, // Simulate network delay in milliseconds
};
